version: '3'
services:
  app:
    build:
      context: ./devops/docker
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    volumes:
      - .:/app
    environment:
      NODE_ENV: development
    command: npm start
